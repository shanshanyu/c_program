C中的运算符主要有：

()  小括号的优先级最高  .  ->  []    ： .  -> 用来访问结构体或联合体成员的，级别也是1，[] 用来访问数组元素的，级别也是1


&   *   取变量的地址、根据地址取内容，一般语言里面也没这两个，c是底层语言，能操作内存


++ --  自增、自减   一般语言里面没有，只有 c 里面有

+  -   *   /  %     加减乘除、取余，c和 java 中没有 **(乘方)  没有 //(整除),python 中有


<<   >>    左移   右移



>   >=   <   <=   ==  !=    大于、大于等于、小于、小于等于、等于、不等于


&     ^   |  ~  按位于、按位异或、按位或 按位取反，按位取反的优先级比较高


&&   ||   !     逻辑与   逻辑或   逻辑非，逻辑非的优先级标高，是 2


?:   条件运算符，java 和 c 中都有，python 中没有，python中是通过 if..else 来实现条件表达式的功能


=   +=   -=   赋值运算符以及赋值运算符的一些符合运算符   


,   c 中还有一个逗号运算符

